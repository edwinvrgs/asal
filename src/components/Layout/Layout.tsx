import React from 'react';

const Layout = props => {
    return (
        <div>
            Esto deberia ser un NavBar para todas las paginas autenticadas
        </div>
    );
};

export default Layout;
